language: python
python:
  - "3.6"

env:
  - DISPLAY=:0

install: 
  - pip install -r requirements.txt

script:
  - PYTHONPATH=$PWD/src pytest --cov=src/ test
  - black --check --verbose src/.

after_success:
  - coveralls

branches:
  only:
  - master